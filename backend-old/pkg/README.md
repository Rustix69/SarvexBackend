# Shared Go Packages

Milestone 0 status:

- Shared package folders scaffolded (`auth`, `idempotency`, `tracing`, `pgx`, `natsutil`, `snowflake`, `audit`).
- Package implementation begins in later milestones.
